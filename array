#include <stdio.h>
#include <conio.h>
int main()
{
    int n, i;
    printf("Enter n: ");
    scanf("%d", &n);
    
    int a[20];
    printf("Enter n numbers: ");
    for(i=0; i<n; i++)
    {
        scanf("\n%d",&a[i]);
    }
    for(i=0; i<n; i++)
    {
       printf("%d ",a[i]);
    }
    return 0;
}
