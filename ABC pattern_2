#include <stdio.h>
#include <conio.h>

int main()
{
    int n=5, i, j, k;
    char ch;
    
    for (i=1; i<=n; i++)
    {
        ch = 65;
        for(j=0; j<=n-i; j++)
        {
            printf("%c",ch++);
        }
        printf("\n");
    }
    return 0;
}
