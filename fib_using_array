#include <stdio.h>
#include <conio.h>
#include <stdlib.h>
int main()
{
    int a[10]={1,1}, n=9, i;
    for(i=2; i<n; i++)
    {
        a[i] = a[i-1]+a[i-2];
    }
    for(i=0; i<n; i++)
    {
        printf("%d ",a[i]);
    }
    return 0;
}
